# Runtime: pooled Neon PostgreSQL connection; server-only, never NEXT_PUBLIC_.
DATABASE_URL=
# Migrations: direct/unpooled connection to the same database.
DATABASE_URL_UNPOOLED=
# Optional integration tests: a migrated disposable PostgreSQL database/branch.
TEST_DATABASE_URL=
# Store real values in ignored .env.local or deployment secrets, never in Git.
# Midnight wallet, network and proof-provider configuration remain pending.
